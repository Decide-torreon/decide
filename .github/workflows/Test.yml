name: Test

on:
  push:
    branches: [develop, Voting-develop, Voting-Docker, Voting-Despliegue]
  
  

jobs:
  build:
    runs-on: ubuntu-latest
    steps:    
      - name: Run tests
        run: cd decide;coverage run --branch --source=. ./manage.py test voting --keepdb -v 2; coverage xml;
